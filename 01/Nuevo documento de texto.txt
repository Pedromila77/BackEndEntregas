class ProductManager {
    constructor() {
        this.products = []
    };
    getProducts() {
        return this.products
    }

    addProduct(title, description, price, thumbnail, code, stock){


        //let code=1 
        //if(this.products.length>0){
            //id=this.products[this.products.length-1].id + 1
        //};
        let newProduct={
            title, 
            description,
            price, 
            thumbnail,
            code,
            stock,
        }
        this.products.push(newProduct)
    }
}
let products1= new ProductManager()
products1.addProduct(`Gallo Oro`, `Arroz Fino`, `$500`, `Sin Imagen`, `${this.code}`, `20`) 
let products2= new ProductManager()
products1.addProduct(`Amanda`, `Arroz`, `$400`, `Sin Imagen`, `10`) 

console.log(products1.getProducts)
console.log(products2.getProducts)